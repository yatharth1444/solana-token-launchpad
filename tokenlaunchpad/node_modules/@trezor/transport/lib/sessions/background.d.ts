import { TypedEmitter } from '@trezor/utils';
import type { HandleMessageParams, HandleMessageResponse } from './types';
import type { Descriptor } from '../types';
export declare class SessionsBackground extends TypedEmitter<{
    descriptors: Descriptor[];
}> {
    private descriptors;
    private locksQueue;
    private locksTimeoutQueue;
    private lastSessionId;
    constructor({ signal }: {
        signal: AbortSignal;
    });
    handleMessage<M extends HandleMessageParams>(message: M): Promise<HandleMessageResponse<M>>;
    private handshake;
    private enumerateIntent;
    private enumerateDone;
    private acquireIntent;
    private acquireDone;
    private releaseIntent;
    private releaseDone;
    private getSessions;
    private getPathBySession;
    private getPathFromSessions;
    private startLock;
    private clearLock;
    private waitForUnlocked;
    private waitInQueue;
    private filterDisconnectedDevices;
    private success;
    private error;
    dispose(): void;
}
//# sourceMappingURL=background.d.ts.map